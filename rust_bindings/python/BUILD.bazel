package(default_visibility = ["//visibility:public"])

load("@rules_python//python:defs.bzl", "py_binary", "py_library")

# Create a Python library that wraps the extension module
py_library(
    name = "twitter_text",
    data = ["//rust/python-bindings:twitter_text"],
    imports = ["."],
)

# Demo binary
py_binary(
    name = "demo",
    srcs = ["demo.py"],
    deps = [":twitter_text"],
    imports = ["."],
)
