actions:
    - name: "Test all targets"
      triggers:
          push:
              branches:
                  - "main"
          pull_request:
              branches:
                  - "*"
      container_image: "ruby:3.4.1"
      steps:
          - run: "bazel test --config=linux //..."
