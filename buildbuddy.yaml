actions:
    - name: "Test all targets"
      triggers:
          push:
              branches:
                  - "main"
          pull_request:
              branches:
                  - "*"
      container_image: "ubuntu-24.04"
      steps:
          - run: "bazel test --config=linux //... -- -//rust/ruby-bindings/..."
