actions:
    - name: "Test all targets"
      triggers:
          push:
              branches:
                  - "main"
          pull_request:
              branches:
                  - "*"
      container_image: "ubuntu-22.04"
      disk: "30GB"
      steps:
          - run: "bazel test --config=linux //..."
