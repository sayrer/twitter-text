actions:
    - name: "Test all targets"
      triggers:
          push:
              branches:
                  - "main"
          pull_request:
              branches:
                  - "*"
      container_image: "gcr.io/terraform-prod-5633e672/twitter-text-build:latest"
      resource_requests:
          disk: "40GB"
      steps:
          - run: "bazel test --config=linux //..."
