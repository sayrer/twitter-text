actions:
    - name: "Test all targets"
      triggers:
          push:
              branches:
                  - "main"
          pull_request:
              branches:
                  - "*"
      container_image: "docker://eclipse-temurin:23-jdk-jammy"
      steps:
          - run: "bazel test --config=linux //..."
