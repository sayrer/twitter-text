# Stub for llvm_toolchain_llvm (the actual LLVM binaries)
# Used with --override_repository to avoid downloading 1.5GB of LLVM

package(default_visibility = ["//visibility:public"])

filegroup(name = "include", srcs = [])
filegroup(name = "extra_config_site", srcs = [])
filegroup(name = "clang", srcs = [])
filegroup(name = "ld", srcs = [])
filegroup(name = "ar", srcs = [])
filegroup(name = "lib_legacy", srcs = [])
filegroup(name = "bin", srcs = [])
