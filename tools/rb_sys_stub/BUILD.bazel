# Stub for rb-sys crate (avoids needing Ruby in CI)
package(default_visibility = ["//visibility:public"])

# Stub the build script target that's failing
filegroup(
    name = "_bs",
    srcs = [],
)

filegroup(
    name = "rb-sys",
    srcs = [],
)
